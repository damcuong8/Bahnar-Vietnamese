# Core dependencies for SeamlessM4T v2 fine-tuning with FSDP
torch>=2.0.0
transformers>=4.35.0
accelerate>=0.24.0

# Optional but recommended
wandb>=0.15.0
sentencepiece>=0.1.99
protobuf>=3.20.0

# For data processing (if using audio files)
torchaudio>=2.0.0
librosa>=0.10.0
soundfile>=0.12.0

# Utilities
numpy>=1.24.0
tqdm>=4.65.0

# For distributed training
ninja  # For faster compilation

